# Deep System Audit — 2026-06-29

Four parallel read-only audits (backend/commands, NLU/RAG, frontend, data/infra/security).
Live-status checked against the running config.

## Live config status
- JWT secret: custom (NOT default) ✅
- Admin password: **`admin123` (DEFAULT, LIVE)** 🔴
- Webhook auth: api-key set (not fail-open) ✅, but HMAC signature not enabled
- CORS origins: empty ✅

---

## CRITICAL
- **C1 — Admin login `admin`/`admin123`** (`config.py:14`, plaintext compare `auth.py:31`). LIVE. *[NOT fixed — user deferred]*
- C2 — JWT secret default placeholder → forge admin tokens (`config.py:16`). Mitigated (custom secret set).
- C3 — Webhook fail-open if no secret+no api-key. Mitigated (api-key set). HMAC signature not enabled.

## HIGH — security (user deferred this batch)
- **H1 — SSRF chain** (`nlu.py:300-318`, `web_search.py`): `_is_safe_url` is name-only; defeated by redirect-following (`follow_redirects=True`), DNS rebinding (hostname → internal IP), and dotted-short/hex IP forms (`127.1`, `0xA9.0xFE…`). User-reachable via any URL message. *[deferred]*
- **H2 — Unbounded fetch**: `resp.text` loads full body before truncation → memory DoS. *[deferred]*
- **H3 — Blocking sync I/O in async webhook** (httpx/LLM/Redis, no await) → event-loop starvation → OpenWA timeouts → retry duplicates. *[deferred — partially mitigated by 6s search timeout]*
- **H10 — nginx no security headers** (CSP/X-Frame-Options/nosniff) + JWT in localStorage. *[deferred]*

## HIGH — correctness (FIXING)
- H4 — Edit-path attachment bypass: `edit task N status done` marks a requires_attachment task done with no photo (`webhook.py` edit handler / `task_manager.update_task`).
- H5 — Edit-path wrong-task numbering: `edit task N` uses `get_all_tasks` ordering, not the list shown to the admin.
- H6 — Daily-reminder lock starvation: 60s job, 3300s lock TTL → 2nd dept reminder skipped all day (`scheduler.py:254,330`).
- H7 — init_db migration race across 4 workers: `ADD COLUMN` without `IF NOT EXISTS` + no advisory lock → worker startup crash.
- H8 — delete_employee misses `sop_definitions`/`sop_executions` cleanup → IntegrityError 500.
- H9 — SOPDepartment.tsx create/edit form omits weekly/monthly/hourly fields → SOPs that never fire.

## MEDIUM (FIXING the correctness subset)
- KB dead under Groq (zero-vector embeddings → NaN cosine → every trouble request escalates). Route to keyword search when no real embeddings; stop writing zero-vector docs.
- Proof images keyed per-employee not per-task → mis-attribution/loss with 2+ attachment tasks; proofs cleared on non-attachment done.
- Overnight interval SOPs (end<start) never fire; exact-minute slot match drops delayed slots (fire on `>= slot` not-yet-executed within a window).
- URL in an assign message hijacks to web-fetch before intent → assignment lost.
- `verify_task_assign` unvalidated enum (`Priority(...)`/`FollowUpType(...)`) → ValueError aborts assign.
- `paused_until` naive timestamp treated as UTC → auto-resume 5.5h off (IST). Interpret naive in app_timezone.
- `confidence` missing → defaults 1.0 (accepts low-quality); `entities:null` → crash (`entities = parsed.get("entities") or {}`).
- Dedup + scheduler locks fail-open when Redis down → duplicates.
- Frontend: due-date prefill blank (`str(datetime)` not ISO); `Promise.all` masks errors; no ErrorBoundary; Settings poll interval leak; exportTasks no `res.ok` check; error detail swallowed.
- Login no rate-limit; `/health/deep` unauth leaks infra status. *[security-adjacent, deferred]*

## LOW (deferred / tracked)
tz clamp past-midnight double-fire; monthly day_of_month 29-31 never fires in short months; FollowUp re-trigger loop when task done; redis no connection pooling; PII (message body) logged at INFO; SLA text hardcodes "4 hours"; CORS wildcard guard; auth returns 200 instead of 401; nginx `/internal` `/health` reachable; `extract_task_number` matches stray numbers ("server 500 done").

---
Generated by 4 opus subagents. Security HIGH batch (H1-H3, H10) and C1 deferred by user — revisit before any public exposure.
